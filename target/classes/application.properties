# DATABASE
spring.datasource.url=jdbc:mysql://localhost:3307/pelearning
spring.datasource.username=root
spring.datasource.password=

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# THYMELEAF
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# SERVER
server.port=8080

# Default profile (so app starts even without MySQL)
spring.profiles.default=dev

# =============================================
# EMAIL VERIFICATION
# =============================================
# Base URL used in verification links (change in production)
app.base-url=http://localhost:8080

# SMTP (real email sending)
# Configure via environment variables:
# MAIL_HOST, MAIL_PORT, MAIL_USERNAME, MAIL_PASSWORD
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=benzammelfatma@gmail.com
spring.mail.password=ycwm nmah gsdf exmv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.smtp.starttls.required=false
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.checkserveridentity=false
spring.mail.test-connection=false

# ===== QUICK SETUP (replace with your real values) =====
# Recommended: set environment variables MAIL_USERNAME and MAIL_PASSWORD
# MAIL_USERNAME=example@gmail.com
# MAIL_PASSWORD=xxxx xxxx xxxx xxxx (Google App Password)

# =============================================
# FILE UPLOAD
# =============================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
upload.dir=uploads/

# =============================================
# LOGGING
# =============================================
logging.level.org.springframework.security=DEBUG
logging.level.com.elearning=DEBUG

# =============================================
# DATA SQL
# =============================================
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true